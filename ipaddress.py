import ipaddress
import json
from logging.config import dictConfig
from random import *
import yaml
import netaddr
from netaddr import IPNetwork
#for ip in IPNetwork('192.0.2.0'):
    #dict_file=ip
    #print(dict_file)
#with open(r'/Users/jaikumar/Documents/ip.yaml', 'w') as file:
    #documents=yaml.dump(dict_file, file)



k= randint(1,100)
print(k)
